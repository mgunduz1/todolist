<%= form_with(model: todo_list) do |form| %>
  <% if todo_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</h2>

      <ul>
        <% todo_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field py-2">
    <%= form.label :title, class:"fs-5" %>
    <%= form.text_field :title %>
  </div>

    <div class="field">
    <%= form.number_field :user_id, id: :todo_list_user_id, class:"form-control", value: current_user.id, type: :hidden  %>
  </div>

  <div class="actions py-3">
    <%= form.submit class:"btn btn-success btn-md" %>
  </div>
<% end %>
